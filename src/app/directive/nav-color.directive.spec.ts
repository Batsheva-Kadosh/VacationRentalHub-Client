import { NavColorDirective } from './nav-color.directive';

describe('NavColorDirective', () => {
  it('should create an instance', () => {
    const directive = new NavColorDirective();
    expect(directive).toBeTruthy();
  });
});
